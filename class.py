#declaration
A = []
for i in range (0,3,1);
    A. append ([])
    for j in range (0,3,1);
    A [i] append (0)
    #input data
for i in range (0,3,1);
    for j in range (0,3,1)
        A[i][j] = (input ("enter Data at row %d column %d"))
#Trace = sum of elements in the left diagonal
    trace = 0
    for i in ([0][1], [1][1], [2][2])
        for j in 
            if i == j;
                trace += A[i][j]
    print("Trace of the matrix")
#Sum of elements except in left diagonal
    sum = 0
    for i in ([0,3,1])
        for j in (0,3,1)
            if i != j;
                sum += A[1][j]
    print ("sum of elements except the last diagonal =",sum")
#Maximum
    maximum = 0
    for i in range (0,3,1)
        for j in range (0,3,1);
            if A[i][j] > maximum
             maximum = A[i][j]
    print("maximum elements =", maximum)
#mean per row
for i in range (0,3,1)
    rowSum = 0
    for J in range(0,3,1);
        rowSum += A[i][j]
#if mean pass mark is fifty, how many students passed the exam
for i in range(0,3,1)
    rowMean = 0
    for j in range(0,3,1);
        rowMean += A[i][j]
    if rowMean >= 50
        count = count+
